import { useRef } from "react";

const Spinner = () => {
    
return(
    <>
    <div className="w-6 h-6 animate-spin rounded-full border-4 border-white border-b-transparent mx-auto ml-2"></div>
</>
);
};

export default Spinner;